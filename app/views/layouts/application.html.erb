<!DOCTYPE html>
<html lang="en">
  <head>
    <% if Rails.env.production? %>
      <%= render "shared/google_analytics" %>
    <% end %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>
      <% if content_for? :page_title %>
        <%= content_for :page_title %>
      <% else %>
        Get Back the Cash You've Earned | GetYourRefund.org
      <% end %>
    </title>

    <%= render "shared/meta_tags" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <%= content_for :additional_head_content %>
  </head>

  <body class="template--<%
      if content_for? :template_css_class %><%=
        content_for :template_css_class %><%
      else %>homepage<% end
  %>">

  <%= content_for :page %>
  </body>

  <footer class="main-footer">
    <div class="grid">
      <div class="grid__item">
        <div class="text--small main-footer__legal">
          GetYourRefund.org is a free non-profit service delivered by <%= link_to "Code for America", "https://www.codeforamerica.org/" %>.
        </div>
        <div class="main-footer__cfa-logo">
          <a href="https://codeforamerica.org">
            <%= image_tag "cfa-logo.png", class: "image image--cfa-logo", alt: "Code for America" %>
          </a>
        </div>
        <div style="clear:both;"></div>
      </div>
    </div>
  </footer>
</html>
